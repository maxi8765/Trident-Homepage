## Packages
framer-motion | For smooth scroll animations and page transitions
lucide-react | Icon library (already in base, but emphasizing usage)
clsx | For conditional class names
tailwind-merge | For merging tailwind classes

## Notes
Static images are imported from @assets/
API endpoints follow /api/products and /api/inquiries structure
Color palette should match the "Trident" blue/purple branding from the brochure assets
